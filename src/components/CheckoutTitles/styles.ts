import styled from 'styled-components'

export const ShippingTitle = styled.div`
  display: flex;
  flex-direction: row;
  gap: 1em;
  align-items: center;

  h1 {
    font-size: 1.25em;
    font-weight: 400;
  }
`
